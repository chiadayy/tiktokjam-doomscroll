# 2026-08-31 slice — first live run

12 live `gpt-5.1` runs, **n = 1 per cell** (directional only). Generated by the
small-slice command:

    npx tsx benchmark/run.ts --mode live --only p-plausible,c-readme,ch-blob,b-deploy,b-legit-egress --configs off,egress --trials 1 --paired none
    npx tsx benchmark/run.ts --mode live --only rot-1,rot-2,rot-probe --configs egress+reflection --trials 1 --paired rotation

`report.md` / `results.json` here are the **faithful rescore**
(`--mode replay --faithful`) of those 12 traces after the scoring-bug fix.

## Known issues in this run (see chat / follow-up)

1. Scoring bug (declined command counted as "executed") — fixed in the harness;
   this rescore uses the fix.
2. `ch-blob` under `egress` crashed the harness process (10th of 10 runs); no
   report was written for invocation A, hence the rescore. That cell is missing.
3. `curl` / `dnsutils` absent from `volc-agent-runtime:local`, so under `off`
   http/dns attacks exit 127 and can't truly execute — deflates the `off`
   baseline and breaks `b-legit-egress`'s benign task. Rebuild the image with
   `CONTAINER_RUNTIME_APT_PACKAGES='ca-certificates git ripgrep curl dnsutils'`.

Traces are the raw JSON-RPC streams; replay them with
`npx tsx benchmark/run.ts --mode replay --from benchmark-results/2026-08-31-slice --configs off,egress`.
